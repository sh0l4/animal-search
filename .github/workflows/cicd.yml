
on:
  push:
    branches: [main, develop]

jobs:
  build:
    runs-on: self-hosted
  
    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Run Docker
      run: docker build . 
